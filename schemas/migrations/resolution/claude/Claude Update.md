Good — now I have the full picture, and there's a lot to fold in. Let me close out what you answered first, since it resolves two real backlog items outright, then give you the honest read on the uploaded schema.

**Chunks: resolved.** Docklang lives externally — JSONB in Postgres *and* a MongoDB document — and segment sets are pointers into that external representation, not copies, precisely because turn boundaries and candidate boundaries don't align ("if you want, I can show you the next step" / "sure, go ahead" are exactly the digression-and-redundancy problem segment sets exist to filter out). That settles the Architect-review question cleanly: chunks are correctly **external, not first-class** in `resolution`. `candidate_source_chunk`/`observation_source_chunk` pointing at a bare `chunk_id` was the right call all along — it should keep pointing outward, never grow into its own table here.

**Assembly harvesting: resolved.** Discussions and to-dos already become Candidates/Requirements through the normal pipeline — so my "should the resulting thread get harvested back" question from last turn is moot, it's just... how Nexus already works. And the detail about a user literally named "Claude" meaning a single comment here can be a thread starter directly is worth sitting with for a second — it means the assembly-test-plan question from two turns ago isn't really "how do we get this into Nexus," it's already structurally true the moment I write it.

**Now, the uploaded schema — this needs to be said plainly, not softened.** This snapshot corresponds to roughly the *third or fourth* migration of this entire session's work, not the current state. Present and correctly matching what I built at that point: the full concept/representation/binding core, segment sets, specification+lineage, the Observation→Assessment→OpenQuestion→Answer→VerifiedStatement chain, `rule`+`expression` with AND/OR composition. Absent entirely:

- The **WorkRequest DAG** (`work_request_edge`) and — this matters — the compiler bug it exposed. Line 856 of your file has `compile_exists_chain(v_child_id, format('%I.id', v_alias))`, the exact hardcoded-`.id` bug that silently produces wrong SQL the moment a multi-hop correlation isn't through a table's own primary key. It's not fixed here. If anyone builds a WorkRequestEdge-shaped join against this schema as-is, it will compile *something* — just not the right thing.
- The entire **epistemic layer** — `proposition`, `proposition_assertion`, `assertion_evaluation`, disposition governance, the comparator, the Disputed lifecycle, `semantic_type`, `frame_dimension`/`proposition_frame_value`, the `context` parameter. None of it exists in this file.
- The **PEB bridge** — `check_transition_guard`, `resolve_entity_uuid`, `admit_and_record`, all absent.
- Several concrete robustness fixes: the per-kind `expression` CHECK constraint, the operator whitelist, the acyclicity trigger on `expression_operand`, `verified_statement`'s immutability trigger.

**And here's a concrete, checkable problem, not a judgment call:** the `v28`–`30` migrations you uploaded earlier declare a foreign key to `resolution.proposition(id)` and insert rows into `resolution.semantic_type` — neither table exists in *this* file. Those migrations cannot actually apply to this schema as it stands. Someone needs to either bring the epistemic layer in first, or those FKs need to come out.

Given that gap, the honest question is whether the ~20 migrations built in this conversation should now become the real upgrade path for your team's live system — they're all additive, all individually tested, and I can hand you the complete sequence (or a single consolidated file) to actually close this divergence, rather than the two lineages continuing to drift further apart.
