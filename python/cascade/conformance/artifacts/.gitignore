# Evidence bundles — captured NATS event data.
# These are regenerated by running probe.py against live cascade services.
# They contain runtime event payloads that should not be committed.
capture_*.json

# Golden captures are committed as reference fixtures
!golden_capture_*.json

# Keep the gitignore itself
!.gitignore
