{
  "name": "tackle-prompt-sync-srv",
  "version": "1.0.0",
  "description": "PG-to-Redis sync server for tackle.prompts + tackle.tasks. Reads the latest version of each prompt template per role from PostgreSQL and populates Redis keys (prompt:proc:{role}::{slug}, prompt:idx:{role}, prompt:meta:last_updated, task:idx:{role}) so live agents can assemble prompts at launch without hitting PG.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "ioredis": "^5.4.0",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "@types/pg": "^8.20.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
