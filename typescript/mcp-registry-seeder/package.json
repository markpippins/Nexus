{
  "name": "mcp-registry-seeder",
  "version": "1.0.0",
  "description": "Phase-1 command registry seeder \u2014 pulls the live tool surface from tools-aggregator /tools and upserts into mcp.command_registry (PostgreSQL). Produces a drift report (new/missing/changed) on re-seed.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "seed": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "project": "tsx src/project.ts"
  },
  "dependencies": {
    "mcp-types": "file:../mcp-types",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}