// Python emission entry point for the reverse-engineered PEB contract.
// The canonical contract remains in the shared spring subtree; this entry
// point deliberately selects only the Python client emitter.
import "../spring/main.tsp";
