# Command

/semantics semantics_add_concept

## Usage

Add a row to semantics.concept (concept (class)) via the add_ proc. Body uses p_* params (see semantics_meta).

## Parameters

| Name | Type | Required | Description |
|------|------|----------|-------------|
| `p_description` | string | No |  |
| `p_expired_at` | string | No |  |
| `p_name` | string | No |  |

## Returns

JSON object with the tool's response content.

## Source

- **MCP Server**: `semantics-mcp`
- **Tool**: `semantics_add_concept`
