pipelineIntent:
  specification: "v1"
  direction: "external-only"
  processingMode: "execute"
  mutationScope:
    filesystem:
      read: true
      write: "all"
    code:
      write: true
    runtime:
      instrument: true
