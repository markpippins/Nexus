# Image Server Environment Variables

# Port for the image server to listen on (default: 8081)
IMAGE_SERVER_PORT=9081

# Root directory for images (default: images in current directory)
IMAGE_ROOT_DIR=./images

# Log level for the server (default: info)
LOG_LEVEL=info