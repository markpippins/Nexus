[package]
name = "ccnf-verifier"
version = "0.1.0"
edition = "2021"
description = "Independent CCNF contract evaluator — Rust verifier for canonical event records"

[dependencies]
serde_json = "1"
sha2 = "0.10"
hex = "0.4"
unicode-normalization = "0.1"
