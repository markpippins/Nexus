Good. This is the point where the system stops being “a knowledge graph that evolves” and becomes something closer to aversioned semantic reality engine.If you don’t define time properly here, everything you built earlier starts quietly breaking in subtle ways (especially self-healing + governance).So now we lock in:how Nebula represents change over time, forks, simulations, and rollback realitydocs/nebula/nebula-temporal-graph-versioning-and-branching.mdMarkdown# Nebula Temporal Graph Versioning & Branching Model**Status:** Draft**Owner:** Nebula core / temporal graph subsystem**Purpose:** Define how Nebula represents time, versioning, branching, rollback, and parallel semantic realities across evolving graphs.---# 1. OverviewNebula is not a static graph.It is a **temporal semantic system** where:- nodes evolve- edges change- classifications drift- invariants strengthen or weaken- execution outcomes reshape structureThis requires a formal model of:> time, versioning, and branching realities---# 2. Core Principle> There is no single “current” Nebula.There are only:- versions of the graph- branches of interpretation- and execution-consistent timelines---# 3. Temporal ModelNebula introduces a **versioned graph timeline**:t0 → t1 → t2 → t3 → ...Each timestamp represents a **graph snapshot state**.---## 3.1 Temporal Graph DefinitionGraphState {version_idparent_version_idtimestampnodes[]edges[]review_state_snapshotvalidation_snapshot}---# 4. Branching ModelA branch represents divergence in semantic evolution.---## 4.1 Branch structureBranch {branch_idbase_version_idhead_version_idpurposestability_score}---## 4.2 Branch types### MAIN- canonical system state- production execution source### EXPERIMENTAL- used for testing mutations or new invariants- unsafe execution allowed### REPAIR- used for healing inconsistent or broken graphs### SIMULATION- hypothetical execution paths- no real mutations applied---# 5. Version Creation RulesA new version is created when:- a mutation is approved- a validation change occurs- a structural repair is applied- a significant execution cycle completes- governance updates are applied---## 5.1 Version creation pipeline```pseudo id="ver1"function create_version(previous_state, mutations):new_state = clone(previous_state)apply_mutations(new_state, mutations)validate(new_state)assign_version_id(new_state)link_to_parent(previous_state, new_state)return new_state6. Temporal Mutation TrackingEvery mutation is time-bound:MutationRecord {mutation_idapplied_at_versioncreated_at_versionaffected_nodes[]}``` id="mut2"This allows:- rollback to pre-mutation state- diffing across time- causality tracking---# 7. Graph Diff EngineNebula supports structural diffs between versions.---## 7.1 Diff types- NODE_ADDED- NODE_REMOVED- NODE_MODIFIED- EDGE_ADDED- EDGE_REMOVED- CLASSIFICATION_CHANGED- REVIEW_STATE_CHANGED---## 7.2 Diff computation```pseudo id="diff1"function diff(v1, v2):return {added_nodes,removed_nodes,modified_nodes,added_edges,removed_edges,state_changes}8. Rollback SystemNebula supports deterministic rollback.8.1 Rollback rulesRollback produces a new version:pseudofunction rollback(target_version):base = load(target_version)new_version = clone(base)mark_as_rollback(new_version)return new_version8.2 Rollback constraintscannot rollback across incompatible schema versionsgovernance approval required for MAIN branch rollbackexecution receipts preserved across rollback boundaries9. Simulation ModeSimulation createsnon-persistent branches.9.1 Simulation propertiesno mutation persistencefull execution allowedfull validation appliedno impact on MAIN branch9.2 Simulation DAG executionpseudorun_execution(dag, mode=SIMULATION)Outputs:hypothetical receiptspredicted failure pointsstability projection10. Temporal Stability ModelEach branch has a stability score:stability =execution_success_rate- invariant_violation_rate- mutation_churn_rate+ validation_consistency``` id="stab2"---## 10.1 Branch promotion rulesA branch may be promoted to MAIN if:- stability > threshold- validation passes consistently- governance approves transition---# 11. Causality TrackingNebula tracks causality across time:NodeVersionHistory {node_idversions[]mutation_chain[]}This enables:- “why did this node become like this?”- full historical traceability- regression debugging across versions---# 12. Temporal Query ModelQueries can specify time:---## 12.1 Query modes- CURRENT → latest MAIN branch- SNAPSHOT(t) → fixed version- BRANCH(id) → branch-specific view- DIFF(v1, v2) → comparison view- SIMULATION → hypothetical execution---# 13. Execution Over TimeConduit execution binds to a version:- execution always references a GraphState- receipts are tied to version_id- reruns may produce new version branches---# 14. Conflict Resolution Across TimeWhen contradictions appear:Priority:1. newer MAIN branch overrides older2. validated SIMULATION cannot override MAIN3. REPAIR branch requires governance approval4. experimental branches never override MAIN---# 15. Key System Property> Nebula is not a graph. It is a branching timeline of graphs with governed divergence and controlled reconciliation.---# 16. Full System Architecture (Final Expanded Form)``` id="arch1"WRP (correctness rules)↓Nebula (temporal semantic graph system)↓Retrieval (role-aware + time-aware selection)↓Conduit (execution runtime bound to version)↓WRP Validation Engine (pre-execution proof gate)↓Self-Healing Layer (mutation proposals)↓Governance Layer (mutation authority)↓Temporal Versioning System (this layer)↓Updated GraphState↓(loop across time)``` id="loop5"---# 17. Final Principle> A system that evolves must also remember every possible way it evolved.Time is not metadata.Time is **structure**.What you now have (system completion + temporal closure)You now have a fully specified system with:1. WRPcorrectness constraints2. Nebula (now temporal)semantic graph + trust + review + version history3. Retrieval systemrole + time aware cognition4. Conduit runtimeexecution over versioned graphs5. Validation enginepre-execution proof system6. Self-healing systempost-execution learning7. Governance modelcontrolled mutation authority8. Temporal versioning system (this doc)branching reality, rollback, simulationWhat you’ve actually built (stepping back)This is no longer a pipeline.It is:Aversioned, self-modifying, role-aware semantic execution operating system with governed mutation and temporal branchingIf we continue (final frontier options)There are only two meaningful next expansions:A. Multi-agent cognitive runtime layerBuilder / Architect / Inspector running concurrently over same versioned graphB. Distributed Conduit execution fabrichorizontal scaling, sharding, parallel graph executionIf you want to continue, the next artifact would be:“Nebula Multi-Agent Cognitive Execution Fabric”That’s where the system stops being single-runtime and becomes acoordinated reasoning swarm over a versioned semantic universe.

Good. This is the point where the system stops being “a knowledge graph that evolves” and becomes something closer to aversioned semantic reality engine.If you don’t define time properly here, everything you built earlier starts quietly breaking in subtle ways (especially self-healing + governance).So now we lock in:how Nebula represents change over time, forks, simulations, and rollback realitydocs/nebula/nebula-temporal-graph-versioning-and-branching.mdMarkdown# Nebula Temporal Graph Versioning & Branching Model**Status:** Draft**Owner:** Nebula core / temporal graph subsystem**Purpose:** Define how Nebula represents time, versioning, branching, rollback, and parallel semantic realities across evolving graphs.---# 1. OverviewNebula is not a static graph.It is a **temporal semantic system** where:- nodes evolve- edges change- classifications drift- invariants strengthen or weaken- execution outcomes reshape structureThis requires a formal model of:> time, versioning, and branching realities---# 2. Core Principle> There is no single “current” Nebula.There are only:- versions of the graph- branches of interpretation- and execution-consistent timelines---# 3. Temporal ModelNebula introduces a **versioned graph timeline**:t0 → t1 → t2 → t3 → ...Each timestamp represents a **graph snapshot state**.---## 3.1 Temporal Graph DefinitionGraphState {version_idparent_version_idtimestampnodes[]edges[]review_state_snapshotvalidation_snapshot}---# 4. Branching ModelA branch represents divergence in semantic evolution.---## 4.1 Branch structureBranch {branch_idbase_version_idhead_version_idpurposestability_score}---## 4.2 Branch types### MAIN- canonical system state- production execution source### EXPERIMENTAL- used for testing mutations or new invariants- unsafe execution allowed### REPAIR- used for healing inconsistent or broken graphs### SIMULATION- hypothetical execution paths- no real mutations applied---# 5. Version Creation RulesA new version is created when:- a mutation is approved- a validation change occurs- a structural repair is applied- a significant execution cycle completes- governance updates are applied---## 5.1 Version creation pipeline```pseudo id="ver1"function create_version(previous_state, mutations):new_state = clone(previous_state)apply_mutations(new_state, mutations)validate(new_state)assign_version_id(new_state)link_to_parent(previous_state, new_state)return new_state6. Temporal Mutation TrackingEvery mutation is time-bound:MutationRecord {mutation_idapplied_at_versioncreated_at_versionaffected_nodes[]}``` id="mut2"This allows:- rollback to pre-mutation state- diffing across time- causality tracking---# 7. Graph Diff EngineNebula supports structural diffs between versions.---## 7.1 Diff types- NODE_ADDED- NODE_REMOVED- NODE_MODIFIED- EDGE_ADDED- EDGE_REMOVED- CLASSIFICATION_CHANGED- REVIEW_STATE_CHANGED---## 7.2 Diff computation```pseudo id="diff1"function diff(v1, v2):return {added_nodes,removed_nodes,modified_nodes,added_edges,removed_edges,state_changes}8. Rollback SystemNebula supports deterministic rollback.8.1 Rollback rulesRollback produces a new version:pseudofunction rollback(target_version):base = load(target_version)new_version = clone(base)mark_as_rollback(new_version)return new_version8.2 Rollback constraintscannot rollback across incompatible schema versionsgovernance approval required for MAIN branch rollbackexecution receipts preserved across rollback boundaries9. Simulation ModeSimulation createsnon-persistent branches.9.1 Simulation propertiesno mutation persistencefull execution allowedfull validation appliedno impact on MAIN branch9.2 Simulation DAG executionpseudorun_execution(dag, mode=SIMULATION)Outputs:hypothetical receiptspredicted failure pointsstability projection10. Temporal Stability ModelEach branch has a stability score:stability =execution_success_rate- invariant_violation_rate- mutation_churn_rate+ validation_consistency``` id="stab2"---## 10.1 Branch promotion rulesA branch may be promoted to MAIN if:- stability > threshold- validation passes consistently- governance approves transition---# 11. Causality TrackingNebula tracks causality across time:NodeVersionHistory {node_idversions[]mutation_chain[]}This enables:- “why did this node become like this?”- full historical traceability- regression debugging across versions---# 12. Temporal Query ModelQueries can specify time:---## 12.1 Query modes- CURRENT → latest MAIN branch- SNAPSHOT(t) → fixed version- BRANCH(id) → branch-specific view- DIFF(v1, v2) → comparison view- SIMULATION → hypothetical execution---# 13. Execution Over TimeConduit execution binds to a version:- execution always references a GraphState- receipts are tied to version_id- reruns may produce new version branches---# 14. Conflict Resolution Across TimeWhen contradictions appear:Priority:1. newer MAIN branch overrides older2. validated SIMULATION cannot override MAIN3. REPAIR branch requires governance approval4. experimental branches never override MAIN---# 15. Key System Property> Nebula is not a graph. It is a branching timeline of graphs with governed divergence and controlled reconciliation.---# 16. Full System Architecture (Final Expanded Form)``` id="arch1"WRP (correctness rules)↓Nebula (temporal semantic graph system)↓Retrieval (role-aware + time-aware selection)↓Conduit (execution runtime bound to version)↓WRP Validation Engine (pre-execution proof gate)↓Self-Healing Layer (mutation proposals)↓Governance Layer (mutation authority)↓Temporal Versioning System (this layer)↓Updated GraphState↓(loop across time)``` id="loop5"---# 17. Final Principle> A system that evolves must also remember every possible way it evolved.Time is not metadata.Time is **structure**.What you now have (system completion + temporal closure)You now have a fully specified system with:1. WRPcorrectness constraints2. Nebula (now temporal)semantic graph + trust + review + version history3. Retrieval systemrole + time aware cognition4. Conduit runtimeexecution over versioned graphs5. Validation enginepre-execution proof system6. Self-healing systempost-execution learning7. Governance modelcontrolled mutation authority8. Temporal versioning system (this doc)branching reality, rollback, simulationWhat you’ve actually built (stepping back)This is no longer a pipeline.It is:Aversioned, self-modifying, role-aware semantic execution operating system with governed mutation and temporal branchingIf we continue (final frontier options)There are only two meaningful next expansions:A. Multi-agent cognitive runtime layerBuilder / Architect / Inspector running concurrently over same versioned graphB. Distributed Conduit execution fabrichorizontal scaling, sharding, parallel graph executionIf you want to continue, the next artifact would be:“Nebula Multi-Agent Cognitive Execution Fabric”That’s where the system stops being single-runtime and becomes acoordinated reasoning swarm over a versioned semantic universe.

Good. This is the point where the system stops being “a knowledge graph that evolves” and becomes something closer to aversioned semantic reality engine.

If you don’t define time properly here, everything you built earlier starts quietly breaking in subtle ways (especially self-healing + governance).

So now we lock in:

> how Nebula represents change over time, forks, simulations, and rollback reality

how Nebula represents change over time, forks, simulations, and rollback reality

---

# docs/nebula/nebula-temporal-graph-versioning-and-branching.md

```
# Nebula Temporal Graph Versioning & Branching Model**Status:** Draft  **Owner:** Nebula core / temporal graph subsystem  **Purpose:** Define how Nebula represents time, versioning, branching, rollback, and parallel semantic realities across evolving graphs.---# 1. OverviewNebula is not a static graph.It is a **temporal semantic system** where:- nodes evolve- edges change- classifications drift- invariants strengthen or weaken- execution outcomes reshape structureThis requires a formal model of:> time, versioning, and branching realities---# 2. Core Principle> There is no single “current” Nebula.There are only:- versions of the graph- branches of interpretation- and execution-consistent timelines---# 3. Temporal ModelNebula introduces a **versioned graph timeline**:
```

```
# Nebula Temporal Graph Versioning & Branching Model**Status:** Draft  **Owner:** Nebula core / temporal graph subsystem  **Purpose:** Define how Nebula represents time, versioning, branching, rollback, and parallel semantic realities across evolving graphs.---# 1. OverviewNebula is not a static graph.It is a **temporal semantic system** where:- nodes evolve- edges change- classifications drift- invariants strengthen or weaken- execution outcomes reshape structureThis requires a formal model of:> time, versioning, and branching realities---# 2. Core Principle> There is no single “current” Nebula.There are only:- versions of the graph- branches of interpretation- and execution-consistent timelines---# 3. Temporal ModelNebula introduces a **versioned graph timeline**:
```

t0 → t1 → t2 → t3 → ...

```
Each timestamp represents a **graph snapshot state**.---## 3.1 Temporal Graph Definition
```

```
Each timestamp represents a **graph snapshot state**.---## 3.1 Temporal Graph Definition
```

GraphState {version_idparent_version_idtimestampnodes[]edges[]review_state_snapshotvalidation_snapshot}

```
---# 4. Branching ModelA branch represents divergence in semantic evolution.---## 4.1 Branch structure
```

```
---# 4. Branching ModelA branch represents divergence in semantic evolution.---## 4.1 Branch structure
```

Branch {branch_idbase_version_idhead_version_idpurposestability_score}

```
---## 4.2 Branch types### MAIN- canonical system state- production execution source### EXPERIMENTAL- used for testing mutations or new invariants- unsafe execution allowed### REPAIR- used for healing inconsistent or broken graphs### SIMULATION- hypothetical execution paths- no real mutations applied---# 5. Version Creation RulesA new version is created when:- a mutation is approved- a validation change occurs- a structural repair is applied- a significant execution cycle completes- governance updates are applied---## 5.1 Version creation pipeline```pseudo id="ver1"function create_version(previous_state, mutations):    new_state = clone(previous_state)    apply_mutations(new_state, mutations)    validate(new_state)    assign_version_id(new_state)    link_to_parent(previous_state, new_state)    return new_state
```

```
---## 4.2 Branch types### MAIN- canonical system state- production execution source### EXPERIMENTAL- used for testing mutations or new invariants- unsafe execution allowed### REPAIR- used for healing inconsistent or broken graphs### SIMULATION- hypothetical execution paths- no real mutations applied---# 5. Version Creation RulesA new version is created when:- a mutation is approved- a validation change occurs- a structural repair is applied- a significant execution cycle completes- governance updates are applied---## 5.1 Version creation pipeline```pseudo id="ver1"function create_version(previous_state, mutations):    new_state = clone(previous_state)    apply_mutations(new_state, mutations)    validate(new_state)    assign_version_id(new_state)    link_to_parent(previous_state, new_state)    return new_state
```

---

# 6. Temporal Mutation Tracking

Every mutation is time-bound:

```
MutationRecord {  mutation_id  applied_at_version  created_at_version  affected_nodes[]}``` id="mut2"This allows:- rollback to pre-mutation state- diffing across time- causality tracking---# 7. Graph Diff EngineNebula supports structural diffs between versions.---## 7.1 Diff types- NODE_ADDED- NODE_REMOVED- NODE_MODIFIED- EDGE_ADDED- EDGE_REMOVED- CLASSIFICATION_CHANGED- REVIEW_STATE_CHANGED---## 7.2 Diff computation```pseudo id="diff1"function diff(v1, v2):    return {        added_nodes,        removed_nodes,        modified_nodes,        added_edges,        removed_edges,        state_changes    }
```

```
MutationRecord {  mutation_id  applied_at_version  created_at_version  affected_nodes[]}``` id="mut2"This allows:- rollback to pre-mutation state- diffing across time- causality tracking---# 7. Graph Diff EngineNebula supports structural diffs between versions.---## 7.1 Diff types- NODE_ADDED- NODE_REMOVED- NODE_MODIFIED- EDGE_ADDED- EDGE_REMOVED- CLASSIFICATION_CHANGED- REVIEW_STATE_CHANGED---## 7.2 Diff computation```pseudo id="diff1"function diff(v1, v2):    return {        added_nodes,        removed_nodes,        modified_nodes,        added_edges,        removed_edges,        state_changes    }
```

---

# 8. Rollback System

Nebula supports deterministic rollback.

---

## 8.1 Rollback rules

Rollback produces a new version:

```
function rollback(target_version):    base = load(target_version)    new_version = clone(base)    mark_as_rollback(new_version)    return new_version
```