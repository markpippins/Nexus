"""Runnable examples for SOLScript.

Each module builds a self-contained interpreter and demonstrates one slice of
the resolution language.  `python -m solscript.examples.role_lease` is the
frame-scoped lease example.
"""
