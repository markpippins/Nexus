# File System Server Environment Variables

# Port for the file system server to listen on (default: 4040)
FS_SERVER_PORT=4040

# Root directory for file system operations (default: fs_root in current directory)
FS_ROOT_DIR=./fs_root

# Log level for the server (default: info)
LOG_LEVEL=info