# 0.1 — Hardcoded Port/Credential Inventory (T25 Phase 0 Recon)

> Date: 2026-08-16 · Produced by engineer (re-produced on behalf of devops — see
> reconciliation record `5d5f890d`). Method: grep sweep of `typescript/*-srv`,
> `scripts/`, and `bin/` for hardcoded ports, DB credentials, and URLs.

## Postgres credentials — hardcoded dev defaults (committed)

The literal `pguser` / `pgpass` is the hardcoded fallback DSN across the
TypeScript `*-srv` DB clients:

```
process.env.<X> || 'postgresql://pguser:pgpass@localhost:5432/nexus'
```

Affected services (≥ 12): assembly-srv, cascade-srv, conduit-srv, execution-srv,
harness-srv, kernel-srv, knowledge-srv, nebula-srv, peb-srv, role-memory-srv,
semantics-srv, tackle-srv (and others). Each is overridable via env
(`PGUSER`/`PGPASSWORD`/`PGHOST`/`PGPORT`/`PGDATABASE` or `DB_*` variants). The
literal is a dev default, not a live secret — but it is committed and shared.

## MongoDB credentials — hardcoded AND committed

- `bin/start-mongodb-docker.sh`: `MONGO_INITDB_ROOT_USERNAME=mongoUser`,
  `MONGO_INITDB_ROOT_PASSWORD=somePassword` (weak, committed).
- Also present in: `python/fs/fs-crawler/docker-compose*.yml`,
  `python/fs/fs-crawler/start-mongodb.sh`, `scripts/bash/start-mongodb.sh`,
  `moleculer/nexus-broker/.env.example`, and 2 READMEs.
- `SPRING_DATA_MONGODB_URI=mongodb://mongoUser:somePassword@localhost:27017/nexus?authSource=admin`
  (gitignored `nexus/.env`; consumed by broker-gateway via `EnvironmentFile`).

## Google credentials (see 2.3 investigation record `d80aeb81`)

- `GOOGLE_API_KEY`, `GOOGLE_SEARCH_API_KEY`, `GOOGLE_SEARCH_ENGINE_ID` in
  `nexus/.env` (gitignored) **and** `moleculer/search/.env` (**committed** — a
  live key is in git history; rotate it).

## Hardcoded ports (top literal `localhost:<port>` refs in `typescript/*-srv`)

| port | refs | likely meaning |
|------|-----:|----------------|
| 3000 | 184 | legacy dev-server default |
| 8080 | 34  | scaffold/example default |
| 8000 | 34  | scaffold/example default |
| 1234 | 34  | scaffold/example default |
| 3101 | 19  | nebula-srv |
| 4567 | 16  | conduit dev server |
| 5432 | 15  | postgres |
| 5173 | 15  | vite dev server |
| 6379 | 11  | redis |
| 3100 / 3500 / 3160 / 3107 / 11434 / 4222 | 1–4 | conduit-mcp / role-memory / semantics / assembly / ollama / nats |

## Notable

- Terrain's `Server` entity carries `sysUser` / `sysPass` fields (host
  provisioning credentials) — flag for the 2.3 credentials-out-of-repo scope.
- `localhost:3000` ×184 is dominated by stale dev-server defaults in docs/tests,
  not live routing config.

## Refresh 2026-08-18 (builder intake, plan 1292)

- **`pguser`/`pgpass` hardcoded fallback now found in 59 files** under `typescript/`
  (original sweep counted ≥12 — widen scope: every `*-srv` DB client carries the
  literal fallback DSN; all overridable via env). This is the 2.3 (credentials
  out of repo) blast radius.
- **Resolved since the original sweep:** Quarkus + Spring broker-gateways moved to
  env-driven `SERVICE_REGISTRY_URL`/`SERVICE_HOST` (commit `9ea0b15c`,
  `broker-fleet.env.example` added). Those two are no longer hardcoded.
- Terrain `Server.sysUser`/`sysPass` credential-in-schema smell unchanged (still
  in scope for 2.3).
- Mongo weak defaults (`mongoUser`/`somePassword`) still committed across
  `start-mongodb*.sh` / docker-compose files (unchanged).
